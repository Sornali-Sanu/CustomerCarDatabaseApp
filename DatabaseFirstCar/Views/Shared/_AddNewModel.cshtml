@model DatabaseFirstCar.Model
<div class="form-group-row">
    <div class="col-8">
        @Html.DropDownListFor(x => x.ModelId, ViewBag.models as SelectList, "----Select---", htmlAttributes: new { @class = "form-control mb-2" })

    </div>
    <div class="col-2">
        <a href="#" id="btnDeleteQualification" class="btn btn-danger">Delete</a>
    </div>
</div>
